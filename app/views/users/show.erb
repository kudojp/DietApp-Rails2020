<h1>
  <%= @user.name + " (" + @user.account_id + ")" %><%= render partial: 'users/follow_form', locals: {user: @user}%>
</h1>
<h2>Profile</h2>
<div><%= show_sex(@user) %></div>
<div><%= show_weight(@user) %></div>
<div><%= show_height(@user) %></div>
<div><%= show_comment(@user) %></div>

<%= link_to "Followings", followings_user_path(@user) %>
<%= link_to "Followers", followers_user_path(@user) %>

<h2>Meal_Posts</h2>
<%= render template: 'meal_posts/index', locals: {meal_posts: @meal_posts} %>
