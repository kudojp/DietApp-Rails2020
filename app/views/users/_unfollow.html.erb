<%= form_for(current_user.active_relationships.find_by(followed_id: user.id),
    html: {method: :delete}, data: {user_id: user.id}, remote: true) do |f| %>
    <%= f.submit "Unfollow", class: 'btn btn-secondary'%>
<% end %>